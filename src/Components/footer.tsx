import React from 'react';
import { useSelector } from 'react-redux';
import { RootState } from '../app/store';

//TODO this will show the "stats" of your current project
//TODO Needs to get details of project.  finsihed/unfinished tickets, each priority level etc etc

const Footer = () => {
	return (
		<>
			<div className="footer"></div>
		</>
	);
};

export default Footer;
